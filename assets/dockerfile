# Use a imagem Nginx como base
FROM nginx:alpine

# Crie e defina o diretório de trabalho
WORKDIR /usr/share/nginx/html

# Copie os arquivos necessários para o contêiner
COPY index.html .
COPY css/ ./css/
COPY JS/ ./JS/

# Exponha a porta 7070 (ou a porta que o seu aplicativo usa)
EXPOSE 7070
